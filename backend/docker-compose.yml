services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: smartrecruit
      POSTGRES_PASSWORD: smartrecruit
      POSTGRES_DB: smartrecruit
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
